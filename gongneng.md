# 功能清单（当前已实现）

本文档基于当前代码状态整理，目标是让你快速判断项目已具备的能力和下一步完善方向。

## 1. 系统定位
- 局域网内的文档/文件共享与协同平台。
- 支持 Word/Excel/PPT 在线编辑（通过 OnlyOffice）。
- 角色分为普通用户与管理员（`is_staff` / `is_superuser`）。

## 2. 技术与运行

### 2.1 后端
- Django 4.2 + DRF + JWT 认证。
- 应用模块：`accounts`、`documents`、`files`、`shares`。
- 媒体文件本地存储（`backend/media/`）。

### 2.2 前端
- Vue 3 + TypeScript + Vite + Element Plus。
- 页面包含登录注册、文档、文件、分享、管理后台。

### 2.3 一键启动
- 根目录 `start.sh` 支持：
  - 自动创建并激活 `venv`
  - 自动安装后端依赖
  - 自动执行迁移与创建测试用户
  - 可选启动前端
- 已优化 `Ctrl+C` 退出行为：
  - 自动停止后端/前端进程
  - 自动清理 8000/3000 端口占用
  - 后端使用 `--noreload` 减少残留进程问题

## 3. 用户与权限功能

### 3.1 认证与账户
- 注册、登录、获取当前用户信息、修改密码。
- JWT 鉴权（Bearer Token）。

### 3.2 用户信息扩展
- 支持组织架构字段：
  - 单位（Organization）
  - 部门（Department，可父子层级）
  - 用户扩展档案（UserProfile）

### 3.3 基础权限模型
- 普通用户：使用文档/文件/分享功能。
- 管理员（`is_staff`）：进入管理后台与管理接口。
- 超级管理员（`is_superuser`）：可执行更高风险操作（例如删除用户、删除组织/部门等）。

## 4. 文档管理（Document）

### 4.1 基本能力
- 创建文档（Word/Excel/PPT 类型）。
- 文档列表与搜索、排序。
- 文档详情查看与更新。
- 软删除与恢复。

### 4.2 协作权限
- 用户可看到：
  - 自己创建的文档
  - 他人分享给自己的文档
- 编辑器配置接口会根据分享权限判断：
  - `write` -> 可编辑
  - `read` -> 只读

### 4.3 OnlyOffice 集成
- 提供编辑器配置接口（文档 URL、key、callback、用户信息）。
- 支持 OnlyOffice 回调保存。
- 支持 JWT 开关配置（可在 `.env` 控制）。
- 已修复文档版本/会话相关问题，降低“文件版本变化需重载”误报。

## 5. 文件管理（File）

### 5.1 基本能力
- 上传、列表、搜索、下载。
- 软删除与恢复。
- 文件列表可展示大小、类型、上传者、上传时间。

### 5.2 在线编辑转换
- 对可编辑类型（doc/docx/xls/xlsx/ppt/pptx 等）支持“一键在线编辑”。
- 后端会将上传文件转换为可编辑 `Document` 并跳转编辑页。

### 5.3 复用优化（已完成）
- 同一用户反复打开同一文件时，会复用已创建的在线文档映射（`FileEditableDocument`）。
- 避免重复创建大量副本文档。

## 6. 分享协作（Share）

### 6.1 分享能力
- 支持分享文档或文件给指定用户。
- 权限支持：`read` / `write`。
- 支持过期时间、启用/停用分享。
- 支持“我分享的”和“分享给我的”两类列表。

### 6.2 你提出的关键场景（已实现）
- 场景：上级把同一模板分发给多个下级，每个下级填写自己的数据，不改原模板。
- 已新增接口：`POST /api/shares/{id}/create-copy/`
- 行为：
  - 仅被分享者可调用
  - 基于该分享创建“当前用户自己的可编辑副本文档”
  - 前端“与我分享”页已新增“填写副本”按钮，一键进入个人副本编辑

## 7. 管理后台（Admin）

### 7.1 仪表盘
- 用户、文档、文件、分享总量统计。
- 最近趋势统计、活跃用户、热门资源。

### 7.2 管理能力
- 用户管理：列表、创建、编辑、删除（含权限约束）。
- 组织架构管理：单位、部门、用户组维护。
- 文档审计：查询与删除控制。
- 文件审计：查询与删除控制。
- 分享管理：分享记录查看。

## 8. 配置与安全（当前状态）
- 已支持 `.env` 读取关键配置（`python-decouple`）：
  - `DJANGO_SECRET_KEY`
  - `DEBUG`
  - `ALLOWED_HOSTS`
  - `CORS_ALLOW_ALL_ORIGINS`
  - `ONLYOFFICE_*`
- 在生产模式下增加了保护：
  - 禁止使用默认 `SECRET_KEY`
  - 禁止 `CORS_ALLOW_ALL_ORIGINS=True`

## 9. 测试与构建
- 后端已有基础测试，当前 `python manage.py test` 可通过。
- 前端 `npm run build` 可通过。
- Node 版本已通过 `.nvmrc` 固定到 `20.19.0`。

## 10. 当前仍建议完善（面向商用）

### 10.1 稳定性与并发
- 数据库从 SQLite 升级到 PostgreSQL。
- 部署改为 Gunicorn + Nginx + 反向代理 OnlyOffice。
- 增加 Redis（缓存、限流、异步任务队列）。

### 10.2 业务能力
- 文档模板中心（模板版本、审批发布、模板权限）。
- 批量分发任务（一次分发给多个人并跟踪回收率）。
- 汇总视图（按组织/部门统计填写结果）。
- 审计日志（谁在什么时候查看/编辑/下载了什么）。

### 10.3 安全能力
- 登录失败限制、IP 白名单、2FA（可选）。
- 下载水印/预览水印（防外传）。
- 细粒度权限（按部门/角色/数据域）。

### 10.4 运维能力
- 备份/恢复策略（数据库 + media 文件）。
- 健康检查、错误告警、操作日志归档。
- 离线安装包（局域网无外网环境的一键部署）。

## 11. 你当前可以直接卖的定位建议（MVP）
- 适合 10~50 人规模的小团队内网文档协作。
- 主打卖点：
  - 私有化部署（数据不出内网）
  - Excel/Word 在线编辑
  - 模板分发 + 每人独立副本填写
  - 管理员统一用户与组织管理
- 商用前建议至少完成：PostgreSQL、备份方案、审计日志、部署文档标准化。

